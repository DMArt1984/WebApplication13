@model List<AlertInfo>

@{
    bool IsAdmin = ViewContext.HttpContext.User.IsInRole("Admin") || ViewContext.HttpContext.User.IsInRole("SuperAdmin");
    //var Obj = (Model != null && Model.Count > 0) ? Model.Select(x => x.ServiceObjectId).Distinct() : null;
    //int FilterSO = (Obj != null && Obj.Count() == 1) ? Obj.First() : 0;
    int FilterSO = Convert.ToInt32(ViewData["ServiceObjectId"]);
    int ItemsCount = (Model != null) ? Model.Count : 0;
}

<!-- получение локального времени -->
<script src="~/assets/dm/localtime.js"></script>

<!-- Таблица -->
<div id="accordion2">
    <div class="card mb-2">
        <div class="card-header bg-soft-warning" id="headingThree">
            <h5 class="m-0 font-size-14">
                <a class="collapsed text-danger" data-bs-toggle="collapse" data-parent="#accordion2" href="#collapseThree" aria-expanded="true" aria-controls="collapseThree" onclick="javascript:flipflopClass('alerts');">
                    
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            @if (ItemsCount > 0)
                            {
                                <i id="alerts" class="fas fa-arrow-up"></i>
                                <span>Активные уведомления <span class="badge bg-danger">@ItemsCount</span></span>
                            }
                            else
                            {
                                <span>Нет активных уведомлений</span>
                            }
                        </div>
                        <div>
                            <a class="col btn btn-outline-danger btn-sm edit" title="Все уведомления объекта >>>" asp-action="AlertsList" asp-route-ServiceObjectId="@FilterSO">
                                <i class="fas fa-align-justify"></i> Все уведомления объекта
                            </a>
                            @if (IsAdmin)
                            {
                                <a class="col btn btn-danger btn-sm edit" title="Добавить" asp-action="AlertEdit" asp-route-Id="0" asp-route-ServiceObjectId="@FilterSO" asp-route-pageInfo="true">
                                    <i class="fas fa-plus"></i> Добавить новое уведомление
                                </a>
                            }
                        </div>
                    </div>

                </a>
            </h5>
        </div>
        <div id="collapseThree" class="collapse show" aria-labelledby="headingThree" data-parent="#accordion2">
            <div class="">
                <!--card-body-->
                @if (ItemsCount > 0)
                {
                    <table id="datatable-buttons" class="table table-striped mb-0" style="border-collapse: collapse; border-spacing: 0px; width: 100%;" role="grid" aria-describedby="datatable_info">
                        <thead class="bg-danger text-white">
                            <tr>
                                <th>Дата и время</th>
                                <th>Статус</th>
                                <th>Сотрудник</th>
                                <th>Сообщение</th>
                                <th>Файл</th>
                                @if (IsAdmin)
                                {
                                    <th></th>
                                }
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.OrderByDescending(x => x.DT))
                            {
                            <tr>
                                <td>
                                    <script>document.write(UTC_To_LocalTime('@item.DT'));</script>
                                </td>
                                <td>@item.StatusRus()</td>
                                <td>
                                    @Html.Partial("_cellUser", new UserCell { Id = "0", UserName = item.UserName, Email = item.UserEmail })
                                </td>
                                <td style="max-width:250px; word-break: break-all">
                                    @item.Message
                                </td>
                                <td>@Html.Partial("_FilesV", item.FileLinks)</td>
                                @if (IsAdmin)
                                {
                                    <td>
                                        <div class="d-flex justify-content-end">
                                            <a class="btn btn-outline-light btn-sm edit" title="Редактировать" asp-action="AlertEdit" asp-route-Id="@item.Id" asp-route-ServiceObjectId="@FilterSO" asp-route-pageInfo="true">
                                                <i class="fas fa-pencil-alt px-1"></i> Редактировать
                                            </a>
                                        </div>
                                    </td>
                                }
                            </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">

    function flipflopClass(id) {
        element = document.getElementById(id);
        link = document.getElementById(id);

        // fas fa-plus-circle
        // fas fa-angle-double-right
        if (link)
            link.className = link.className == "fas fa-arrow-up" ? "fas fa-arrow-down" : "fas fa-arrow-up";
    }

</script>


